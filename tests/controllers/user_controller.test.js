const UserController = require("../../controllers/user_controller");

describe("User Controller", () => {
  let req, res;

  describe("Create", () => {
    beforeEach(() => {
      req = {};
      res = {};
    });

    it("", done => {
      done();
    });
  });
});